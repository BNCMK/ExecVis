<?xml version="1.0" encoding="utf-8"?>
<!-- No package sources. This adapter deliberately depends on nothing outside
     the base class library, so a restore should never reach the network; and
     an offline build proves that rather than asserting it. -->
<configuration>
  <packageSources>
    <clear />
  </packageSources>
</configuration>
